rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // app/data — 읽기는 공개 (로그인 전 users 로드 필요), 쓰기는 인증 필수
    match /app/data {
      allow read;
      allow write: if request.auth != null;
    }
    // 로그인 잠금 상태 — 로그인 전에도 읽기/쓰기 필요
    match /app/loginLocks {
      allow read;
    }
    match /app/loginLocks/locks/{loginId} {
      allow read, write;
    }
    // 사용자 프로필
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    // UID 매핑 (progressive migration용 write 허용)
    match /app/userMapping {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    // 설정 (super만 쓰기)
    match /app/settings {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'super';
    }
    // 템플릿
    match /app/templates {
      allow read, write: if request.auth != null;
    }
    // 페이지 컬렉션
    match /pages/{pageId} {
      allow read, write: if request.auth != null;
    }
    // 페이지 버전 서브컬렉션
    match /pages/{pageId}/versions/{verId} {
      allow read, write: if request.auth != null;
    }
    // 페이지 댓글 서브컬렉션
    match /pages/{pageId}/comments/{cmtId} {
      allow read, write: if request.auth != null;
    }
  }
}
